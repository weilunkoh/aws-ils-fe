const beURL = process.env.BE_URL;

export const hostMsgURL = `${beURL}/host_message`;
export const loginURL = `${beURL}/login`;
export const inferenceURL = `${beURL}/inference`;
export const evaluationURL = `${beURL}/evaluation`;
export const trainingURL = `${beURL}/training`;
export const visualisationURL = `${beURL}/visualisation`;
export const allRecordsURL = `${beURL}/all_records`;
export const individualRecordsURL = `${beURL}/individual_records`;
export const pwChangeURL = `${beURL}/password_change`;
export const userAdminURL = `${beURL}/user_admin`;
export const imageURL = `${beURL}/image`;
export const trainingClassURL = `${beURL}/training_class`;
export const centroidURL = `${beURL}/centroid`;
export const validationURL = `${beURL}/validation`;
export const textFeaturesURL = `${beURL}/text_features`;
